<form class="{{cssClass}}" autocomplete="off">
  <header class="sheet-header">
    <img
      class="profile-img"
      src="{{item.img}}"
      data-edit="img"
      title="{{item.name}}"
    />
    <div class="header-fields">
      <h1 class="charname"><input
          name="name"
          type="text"
          value="{{item.name}}"
          placeholder="Nome do Equipamento"
        /></h1>
    </div>
  </header>

  {{! CORPO DA FICHA }}
  <section class="sheet-body">
    <div class="grid grid-3col">
      <div class="resource flex-group-center">
        <label class="resource-label">Quantidade</label>
        <input
          type="text"
          name="system.quantidade"
          value="{{system.quantidade}}"
          data-dtype="Number"
        />
      </div>

      <div class="resource flex-group-center">
        <label class="resource-label">Peso (kg)</label>
        <input
          type="text"
          name="system.peso"
          value="{{system.peso}}"
          data-dtype="Number"
        />
      </div>

      <div class="resource flex-group-center">
        <label class="resource-label">Custo</label>
        <input
          type="text"
          name="system.custo"
          value="{{system.custo}}"
          data-dtype="Number"
        />
      </div>
    </div>

    <label class="resource-label" style="display:flex;align-items:center;gap:6px;margin-top:8px;">
      <input type="checkbox" name="system.canalizador" {{checked system.canalizador}} style="width:auto;" /> Canalizador (orbe/cajado — permite conjurar)
    </label>

    <hr />

    {{! ÁREA DE DESCRIÇÃO }}
    <h3>Descrição</h3>
    <div class="editor-box">
      {{editor
        system.description
        target="system.description"
        rollData=rollData
        button=true
        owner=owner
        editable=editable
      }}
    </div>
  </section>
</form>